/target

# Fetched at build time by scripts/fetch-toolchain.sh — never committed.
# Proprietary MWCC + recompiled SGI IDO (neither may be redistributed) and the
# GPL objdumps (+ the libzstd the mips one links). Only MIT-licensed wibo
# (vendor/toolchain/wibo) is committed.
/vendor/compiler/
/vendor/toolchain/binutils/
/vendor/toolchain/binutils-mips/
/vendor/ido/
